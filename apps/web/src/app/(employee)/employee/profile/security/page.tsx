import { ScaffoldPage } from "@/components/scaffold-page";

export default function ProfileSecurityPage() {
  return (
    <ScaffoldPage
      description="Update your password at /auth/change-password."
      title="Profile — Security"
    />
  );
}
